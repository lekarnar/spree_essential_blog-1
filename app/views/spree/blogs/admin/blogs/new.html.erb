<%= render :partial => 'spree/shared/error_messages', :locals => { :target => @blog } %>

<%= form_for([:admin, @blog]) do |f| %>
  <fieldset>
    <legend style="padding-top:20px;"><%= t('.new_blog') %></legend>
    <%= render "form", :form => f %>
    <%= render :partial => 'spree/admin/shared/new_resource_links' %>
  </fieldset>
<% end %>

<%= render "spree/blogs/admin/shared/select_configuration_tab" unless request.xhr? %>
