<%= render :partial => 'spree/blogs/admin/shared/post_tabs', :locals => {:current => "Images", post: @post} unless request.xhr? %>


<%= render 'spree/shared/error_messages', :target => @object %>

<%= form_for(@object, :url => admin_post_images_path(@post), :html => { :multipart => true }) do |form| %>
  <fieldset>
    <legend><%= Spree.t("new_image") %></legend>
    <%= render "form", :form => form %>
    <%= render :partial => 'spree/admin/shared/new_resource_links' %>
  </fieldset>
<% end %>

<script type="text/javascript">
  jQuery('#cancel_link').click(function (event) {
    jQuery('#new_image_link').show();
    jQuery('#images').html('');
  });
</script>
